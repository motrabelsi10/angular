export class Interaction {
    idInteraction!: number; // Define idInteraction property
    liked!: boolean;
    dislike!: boolean;
    publication!: any; // Update to match the structure of Publication model
    user!: any; // Update to match the structure of User model if available

}
  