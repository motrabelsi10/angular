export class Commentairepub {
    idCommentaire!: number;
    body!: string;
    publication!: any; // Update to match the structure of Event model if available
    user!: any; // Update to match the structure of User model if available
}
