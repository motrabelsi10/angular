import { Interaction } from './interaction'; // Update the path as per your project structure

describe('Interaction', () => {
  it('should create an instance', () => {
    expect(new Interaction()).toBeTruthy();
  });
});
